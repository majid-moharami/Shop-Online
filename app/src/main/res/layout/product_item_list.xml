<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.example.digikala.viewmodel.HomeFragmentViewModel" />
        <variable
            name="position"
            type="Integer" />
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:onClick="@{()->viewModel.onItemSelectedRecentProduct(position)}">

        <androidx.cardview.widget.CardView
            android:id="@+id/product_item"
            android:layout_width="140dp"
            android:layout_height="240dp"
            app:cardCornerRadius="4dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

                <ImageView
                    android:id="@+id/imageView_product_image"
                    android:layout_width="match_parent"
                    android:layout_height="90dp"
                    android:layout_weight="1"
                    android:scaleType="fitCenter"
                    tools:srcCompat="@tools:sample/avatars"
                    />

                <TextView
                    android:id="@+id/textView_product_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_margin="6dp"
                    android:layout_weight="1"
                    android:gravity="start"
                    android:inputType="textMultiLine"
                    android:maxLines="4"
                    android:padding="4dp"
                    android:textColor="#000000"
                    android:textSize="12sp"
                    android:text="@{viewModel.recentProductLiveData.get(position).name}"
                    tools:ignore="TextViewEdits"
                    tools:text="تلوزیون 56 اینچ ایکویژن" />

                <TextView
                    android:id="@+id/textView_product_price"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:gravity="center"
                    android:textColor="#000000"
                    android:textSize="10sp"
                    android:textStyle="bold"
                    android:text="@{viewModel.recentProductLiveData.get(position).price}"
                    tools:ignore="SmallSp"
                    tools:text="26000 تومان" />
            </LinearLayout>


        </androidx.cardview.widget.CardView>
    </LinearLayout>
</layout>